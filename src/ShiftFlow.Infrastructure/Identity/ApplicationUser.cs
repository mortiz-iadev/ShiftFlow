using Microsoft.AspNetCore.Identity;

namespace ShiftFlow.Infrastructure.Identity;

/// <summary>
/// Usuario de Identity de la aplicación (ASP.NET Core Identity).
/// </summary>
public sealed class ApplicationUser : IdentityUser;
